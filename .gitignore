# Secrets — never commit
.env
.env.*
!.env.example
*.key

# OS / editor
.DS_Store

# Node
node_modules/
dist/

# Python
__pycache__/
.venv/
*.pyc

# Run outputs (demo artifacts regenerate on each run)
runs/

# Session/local state
.remember/
.claude/settings.local.json
.dev_staging/
.deploy_build/

# Local writable profile store (bundled default stays pristine)
data/company_profile.local.json
scratch_video/

# Claude Code internal artifacts
.claude/

data/users.local.json
data/profiles.local/
